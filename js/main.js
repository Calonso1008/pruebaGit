/*Preguntar:
-Si se puede programar los eventos desde el js
-Cross origin request are only support protocol scemes: el dominio en el que esta el sitio no es el mismo dominio que el datos/texto
servidor web WampServer,www root de loar proyectos
*/
/*funcion load*/
function cargar(){
	$("#texto").load("../datos/texto.txt");
	alert("todo tuanis");
	
}
